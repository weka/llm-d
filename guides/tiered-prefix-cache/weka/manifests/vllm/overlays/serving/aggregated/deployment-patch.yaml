# Aggregated mode: 9 replicas, no --kv-transfer-config (LMCache-free by default).
# Mixins append --kv-transfer-config when LMCache is needed.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-d-model-server
spec:
  replicas: 9
  selector:
    matchLabels:
      llm-d.ai/role: aggregated
  template:
    metadata:
      labels:
        llm-d.ai/role: aggregated
        llm-d.ai/model: Llama-33-70B-Instruct
    spec:
      serviceAccountName: weka-vllm
      initContainers:
      - name: create-cufile
        image: quay.io/weka.io/llm-d:old-sha-8c00fce
        imagePullPolicy: Always
        command:
        - /bin/bash
        - -c
        args:
        - curl -fsSL -o /tmp/amgctl https://github.com/4D0R/amg-utils/releases/download/v0.1.21-gds/amgctl-linux-amd64
          && chmod +x /tmp/amgctl && /tmp/amgctl host config cufile /output/cufile.json
        volumeMounts:
        - name: cufile-output
          mountPath: /output
      containers:
      - name: vllm
        args:
        - RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic
        - --port
        - '8000'
        - --served-model-name
        - RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic
        - --tensor-parallel-size
        - '4'
        - --block-size
        - '256'
        - --gpu-memory-utilization
        - '0.80'
        - --disable-log-requests
        - --disable-uvicorn-access-log
        - --max-model-len
        - '125000'
        - --enable-auto-tool-choice
        - --tool-call-parser
        - llama3_json
        resources:
          limits:
            cpu: '16'
            memory: 256Gi
            nvidia.com/gpu: '4'
          requests:
            cpu: '16'
            memory: 256Gi
            nvidia.com/gpu: '4'
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 120
        readinessProbe:
          httpGet:
            path: /v1/models
            port: 8000
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
